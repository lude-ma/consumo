[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "consumo-common"
version = "0.1.0"
description = "Shared models and config for Consumo"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
